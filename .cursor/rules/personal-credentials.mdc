---
description: Personal-only GitHub/Vercel credentials for this portfolio (never AutoLeap)
alwaysApply: true
---

# Personal project — credential guardrails

This repo is **Hassaan Ahmed's personal portfolio** (`HassaanAhmed97/resume-chatbot`). It is not an AutoLeap project.

## Never use machine-default credentials

- Do **not** use `gh` CLI, git HTTPS, or Vercel CLI as **hassaan-leap** / AutoLeap
- Do **not** push to AutoLeap GitHub orgs or Vercel projects
- Do **not** run `vercel --prod` from CLI unless confirmed logged in as personal account — prefer `git push` → Vercel auto-deploy

## Use project credentials only

- **GITHUB_TOKEN** from `.env.local` (must resolve to `HassaanAhmed97`)
- Validate before deploy: `npm run validate:token`
- Push: `npm run push:main` (not raw `git push` / `gh`)
- GitHub Pages: `npm run deploy:github` or `npm run deploy:all`
- Set commit author for this repo only:
  `GIT_AUTHOR_NAME="Hassaan Ahmed" GIT_AUTHOR_EMAIL="hassaan.riaz97@gmail.com"`

## Live targets (personal only)

- GitHub Pages: `hassaanahmed97.github.io/resume-chatbot`
- Vercel: `resume-chatbot-three.vercel.app` (project `resume-chatbot`)

For full deploy steps, read `.cursor/skills/deploy/SKILL.md`.
